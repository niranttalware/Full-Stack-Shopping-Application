
-- to show all databasees 
show databases;

-- create new database
create database ShopDB;

-- use existing database
use ShopDB;


-- create products table
CREATE TABLE products (
  id INT AUTO_INCREMENT PRIMARY KEY,
  product_id VARCHAR(50) UNIQUE,   
  title VARCHAR(255) NOT NULL,
  description TEXT,
  price DECIMAL(12,2) NOT NULL,
  category VARCHAR(100) NOT NULL,  
  main_category VARCHAR(100) NOT NULL, 
  rating_rate FLOAT DEFAULT 0,
  rating_count INT DEFAULT 0,
  image TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


-- show all the products from products table 
select * from products;


-- create users table
CREATE TABLE users (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(150) NOT NULL,
  email VARCHAR(255) NOT NULL UNIQUE,
  password VARCHAR(255) NOT NULL,
  city VARCHAR(150),
  role ENUM('user','admin') NOT NULL DEFAULT 'user',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- show all users from users table
select * from users;


-- insert record in users table
INSERT INTO users (name, email, password, city, role)
VALUES ('Admin', 'admin@gmail.com', 'admin@123', 'Delhi', 'admin');


-- delete user from users table
delete from users where email="admin@gmail.com";


-- update role from the users table
UPDATE users SET role = "admin" WHERE email = "admin@gmail.com";


-- to show the specific record from the users table 
SELECT role FROM users WHERE email = "admin@gmail.com";



-- Insert products into Products table
INSERT INTO products (
  product_id, title, description, price, category, main_category,
  rating_rate, rating_count, image
) VALUES (
  'MEN-SHO-001',
  'Running Shoes',
  'Bacca Bucci Men Lace Up Running Shoes',
  1287,
  'shoes',
  'MensWear',
  3.7,
  2958,
  'https://m.media-amazon.com/images/I/61r9oNywMeL._SY695_.jpg'
);


-- to delete the product from products table
Delete from products where category="smartphones";


-- update main_Category from products table 
UPDATE products 
SET main_category = 'MensWear'
WHERE main_category = 'MensClothing';


-- Safe Mode Enable and Disable
SET SQL_SAFE_UPDATES = 0;
SET SQL_SAFE_UPDATES = 1;




